import React from 'react';

function TodoCounter(props) {
    return (
        <h2>Todo Counter Here!</h2>
    );
}

// Its recommended to use named exports to prevent weird naming when the code is imported.
export { TodoCounter };